import styled from 'styled-components';

export const Container = styled.main`
  position: relative;
  top: 8rem;

  max-width: 108rem;
  overflow: hidden;
  margin: 0 auto;
  padding: 0.6rem;
  padding-top: 2rem;
`;
